$(document).ready(function () {

    $('.answer').click(function(){
        
        $('button').attr()
        // alert('button');
    });

    $('.quiz_btn').on('click', function () {
        if ($())
        // $('.answer').hide().text('Real sou real').show(alert('Realnostichno slishkom. poprobovat snova?'));
    });
});
